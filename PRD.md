# ESP32 本地人脸识别门禁演示器 · 产品需求文档（PRD）

> 文档版本：v1.3　｜　建立日期：2026-09-19　｜　v1.3 修订日期：2026-09-22
> 对应阶段：Day 4 建立产品基线；Day 6 前完成暂定值回填
> 上游文档：`research.md`（2026-09-18 定稿）　｜　下游文档：`TECH_DESIGN.md`（实现契约，Day 5 产出、Day 6 前完成回填）
> 本文只定义「做什么」和「做到什么程度算完成」，**不含技术选型与实现方案**（属 Day 5 及之后）。
>
> **v1.1 修订记录**（针对外部评审意见）：
> ① 新增 2.3 关键行为规则；② 新增 2.4 异常路径处理；③ 新增 4.3 测试规程；
> ④ 新增 4.4 验收口径与交付版本；⑤ 5.1 改为明确的 V1-D 降级路径；
> ⑥ 硬件型号、引脚、阈值等具体参数**有意不收进本文**，全部移交给 Day 5 的实现契约。
>
> **v1.2 修订记录**（Day 4 板块③ 自检发现的必改项）：
> ① 4.4 新增「逐条验收载体对照」——修掉 V1-D 要求验证一个不存在的屏幕（A1–A5、A7–A9 原判定载体全是屏幕，降级版一条都验不了）；
> ② 2.1 状态机补入「错误状态」——修掉「仅 4 个状态」与 2.4 异常路径的章节打架；
> ③ 5.2 明确交付版本命名按「实际通过的验收集合」——修掉「V1 必须通过 A1–A9」与「V1 下 A6 可未完成」两种口径并存；
> ④ 新增 4.5 F8 优先级例外——修掉 F8 标为 P1（可降级）与 A6 必须通过之间的矛盾。
>
> **v1.3 修订记录**（暂定值定稿回填，Day 6 前完成）：
> ① 2.3 的 R1–R10 暂定值**全部定稿**并回填本表；
> ② **时序规则口径统一**：R3 / R4 / R5 原文用「连续 N 帧」，现统一改为「连续 N 毫秒」。理由：帧数口径会让同一条规则在不同帧率下变成**不同的实际时长**，而目标帧率尚未实测。换算锚点取 R4 自带的「15 帧 ≈ 1 秒」→ 1 帧 ≈ 66.7 ms；其中 R3「3 帧」→ ≥ 200 ms、R5「3 帧」→ ≥ 200 ms 属**推算**，实测帧率后需复核；
> ③ 4.4 的 A6 模式**定稿**：板载确认为 **1 颗单色 LED**，颜色维度不可用，靠「亮/灭 + 闪烁次数」区分；蜂鸣器时长补为短 100 ms / 长 500 ms；
> ④ 第 6 节提到的实现契约文档正式定名为 **`TECH_DESIGN.md`**，原暂定名 `IMPLEMENTATION_CONTRACT.md` 作废；
> ⑤ 第 7 节状态更新：R1–R10 数值、A6 模式两项由「待定稿」转为**已定稿**；R9 的「检测输出是否包含人脸框面积」已确认**包含**（检测结果含 `box[4]`）。
>
> 本次回填的完整依据与出处见 `TECH_DESIGN.md` 第 4 节；**同一数值若需变动，必须两份文档同时改**。

---

## 0. 一句话定位

在一块 ESP32 摄像头开发板上，做出一个能在**设备本地**完成「采集画面 → 识别 1 名已录入人员 → 屏幕给出结果与反馈」完整闭环的**门禁演示器**。

它要证明的是「这条技术链路在本机跑得通」，**不是**一个可用的门禁产品，也不具备任何真实门禁的安全能力。

它不解决：真实开门控制、多设备联网、云端人脸库、活体检测、商用安全认证。

---

## 1. 目标用户与使用场景

### 1.1 使用者

| 角色 | 是谁 | 关注什么 |
|---|---|---|
| 操作者（主要） | 项目作者本人，零基础初学者 | 能否稳定复现、出错时看得见线索、每天有可验证的进展 |
| 观众 | 同学、老师、评审 | 一眼看懂「它在认人」，并看到成功与失败两种结果 |

### 1.2 核心场景

1. 设备放在桌面上，摄像头朝向人脸。
2. 一个人站到或坐到摄像头前，距离与光照相对固定（约 30–80 cm，室内常规照明）。
3. 设备在屏幕上给出结果。
4. 这个人离开，换另一个人上前。

### 1.3 场景约束

- 只支持**单人依次上前**，不支持画面中多人同时识别并分别判定。
- 光照、距离、角度都会影响结果，本项目不承诺复杂环境下的鲁棒性。
- 演示环境是「光照稳定的室内桌面」，不是走廊、室外或强逆光门口。

---

## 2. 功能范围（本期要做）

优先级说明：**P0** = 不做这个项目就没意义；**P1** = 应该做，允许降级。

| 编号 | 功能 | 优先级 | 用户可见行为 |
|---|---|---|---|
| F1 | 实时画面采集 | P0 | 屏幕持续显示摄像头当前看到的画面，取帧不中断 |
| F2 | 人脸检测 | P0 | 画面中出现人脸时，在屏幕上框出人脸位置 |
| F3 | 单人录入 | P0 | 能录入 1 名人员，录入过程屏幕提示「录入中」及进度 |
| F4 | 人脸识别 | P0 | 已录入人员出现在镜头前，屏幕显示「已识别」与该人员编号 |
| F5 | 未录入人员判定 | P0 | 未录入人员出现在镜头前，屏幕显示「未知」，且不显示任何已录入人员编号 |
| F6 | 识别结果屏幕显示 | P0 | 结果以文字形式显示在屏幕上，字号足够站在 1 米外看清 |
| F7 | 状态指示 | P1 | 屏幕上能看出当前处于哪种状态：待机 / 录入中 / 识别成功 / 识别失败 |
| F8 | 成功/失败硬件反馈 | P1 | 识别成功与失败给出**不同**的 LED 表现和**不同**的蜂鸣器表现，且两者都与屏幕结果一致 |

### 2.1 设备状态机（4 个正常状态 + 1 个错误状态）

下表是产品正常运转时的 4 个状态；初始化失败等非正常情况停在哪个状态，见 2.4。

| 状态 | 屏幕显示 | 如何进入 | 如何退出 |
|---|---|---|---|
| 待机 | 实时画面 +「请看镜头」 | 上电完成 / 录入完成 / 一次判定结束 | 检测到人脸，进入录入或识别流程 |
| 录入中 | 「正在录入 1 号」+ 进度提示 | 上电后首次检测到人脸（此时名单为空） | 采样完成（成功）或人离开画面 / 超时（失败） |
| 识别成功 | 「欢迎，1 号」 | 检测到的人脸与已录入人员匹配 | 该人离开画面 |
| 识别失败 | 「未知人员」 | 检测到的人脸与已录入人员不匹配 | 该人离开画面 |
| **错误**（非正常） | 「摄像头错误」等固定提示 | 摄像头等关键部件初始化失败（运行中内存不足的情形见 2.4） | 断电重新上电（见 2.4） |

### 2.2 数据与生命周期

- 本期**只有 1 名人员**，编号固定为 1 号。
- 名单与识别数据**只存在于内存中，不写入 Flash**；设备断电或重启后名单清空，需要重新录入。
- 没有「删除人员 / 修改人员 / 查看名单」这类管理功能（理由见第 3 节）。

### 2.3 关键行为规则（数值已定稿，依据见 `TECH_DESIGN.md` 第 4 节）

> 这一节定的是**规则**，不是实现参数。规则不写清，同一份 PRD 会做出两种行为完全不同的产品。
> v1.3 起本表数值**已全部定稿**，不再标「暂定」。定稿值一律以 `TECH_DESIGN.md` 第 4 节为准，**不允许在代码里临时决定**。
>
> ⚠️ **口径说明（v1.3 变更）**：R3 / R4 / R5 原文用「连续 N 帧」表达，现统一改为「**连续 N 毫秒**」。理由：帧数口径会把规则绑死在帧率上——「15 帧」在 15fps 下是 1 秒，在 7fps 下就变成 2.1 秒。换算锚点取 R4 自带的「15 帧 ≈ 1 秒」→ **1 帧 ≈ 66.7 ms**；其中由该锚点算出的值标注为**推算**。

| 编号 | 规则 | 定稿值 | 为什么必须有这条 |
|---|---|---|---|
| R1 | 录入采样量 | 连续采集 **10 张**合格人脸后录入完成 | 不定义就不知道「进度」的终点在哪，屏幕进度提示无法实现 |
| R2 | 录入超时 | 进入录入后 **15000 ms** 未完成 → 中止，回待机，屏幕提示「录入超时，请重试」 | 防止长时间卡在录入状态出不来 |
| R3 | 录入中人脸消失 | 连续丢失 **≥ 200 ms** → 中止录入，回待机，屏幕提示「录入中断，请重试」（原「3 帧」，**推算值**） | 避免把录到一半的无效数据当成完整录入 |
| R4 | 「人离开画面」判定 | 连续丢失 **≥ 1000 ms** 才判定离开（原「15 帧（约 1 秒）」，时间值取自原文） | 单帧丢失是常态（眨眼、转头），按单帧判定会让状态疯狂抖动 |
| R5 | 识别需连续匹配 | 连续匹配同一人 **≥ 200 ms**，才显示「识别成功」（原「3 帧」，**推算值**） | 防抖，避免单帧误判直接上屏 |
| R6 | 判定后冷却 | 一次判定结束后 **1000 ms** 内不重新判定 | 避免同一瞬间在成功与未知之间反复横跳 |
| R7 | 同一人停留不重复触发 | 同一次停留只触发一次结果（一次成功或一次失败），停留期间不重复提示 | 否则人站在镜头前不动，屏幕和蜂鸣器就会一直响 |
| R8 | 未知人员不重复报警 | 同一次停留只报一次「未知」，不逐帧重复报警 | 同上，避免噪音式反馈 |
| R9 | 画面出现多张脸 | 只处理检测框面积最大的那一张（面积 = 检测框宽 × 高），其余忽略；不进入错误状态、不显示多个结果 | 场景约束是单人上前，但现实中一定会有别人从背后走过 |
| R10 | 结果锁定 | 判定结果保持到该人离开（按 R4），期间新出现的人脸不改变当前显示 | 保证屏幕稳定可读、能截图取证 |

### 2.4 异常路径处理

> 两条总原则：
> ① **任何异常都不得静默失败**——屏幕上必须有可见提示，不能既不动也不说话。
> ② **任何异常都不得要求重新烧录固件才能恢复**——最多断电重新上电（与 A1、A8 一致）。
> 下表只规定「用户看得见什么、停在哪个状态」；串口打印什么属于开发手段，见第 6 节。

| 异常 | 停在哪个状态 | 屏幕显示 | 恢复正常需要做什么 |
|---|---|---|---|
| 摄像头初始化失败 | 不进入待机，停在错误状态 | 「摄像头错误」+ 固定提示画面 | 断电重新上电；不需要重新烧录 |
| 屏幕初始化失败 | 无法上屏 → 进入 V1-D 降级交付（见 4.4、5.1） | 无屏幕，改用串口输出 | 按 4.4 声明为降级交付并标注 |
| 内存不足 | 停在当前状态，不启动新流程 | 「内存不足，请重试」 | 断电重新上电 |
| 录入超时（R2） | 回到待机 | 「录入超时，请重试」 | 重新站到镜头前，无需断电 |
| 录入中人脸消失（R3） | 回到待机 | 「录入中断，请重试」 | 重新站到镜头前，无需断电 |
| 画面出现多张脸（R9） | 不改变状态 | 显示当前判定对象的人脸框 | 无需处理 |
| 识别结果抖动或判错 | 按 R5、R6 防抖后锁定到该人离开 | 保持锁定结果，不来回跳 | 人离开后重新上前；无需断电 |
| 运行中重启 / 看门狗复位 | 复位后回到待机，名单清空 | 正常待机画面 | 重新录入；不需要重新烧录 |
| 供电不稳导致反复重启 | 反复进入待机 | 每次都能看到待机画面 | 换更稳的供电方式后再演示 |

---

## 3. 砍掉的功能（本期重点）

判断标准只有一条：**它是否服务于「在 28 天内、由零基础的人做出一个可稳定演示的本地闭环」**。不服务这条的，一律砍。

### 3.1 本期主动砍掉，且不打算再做

| 砍掉的功能 | 为什么砍 | 砍了之后缺什么 / 怎么凑合 |
|---|---|---|
| 真实门锁、电机、继电器等开门控制 | 这是唯一会碰到「真实世界」的部分。一旦接上执行器，认错人就不再只是「显示错了」，而是安全隐患；同时额外硬件接线与调试会挤掉识别闭环本身的时间 | 用屏幕 + LED + 蜂鸣器表达「通过 / 不通过」。演示时口头说明「这里换成继电器就是开门」 |
| 活体检测 / 防照片攻击 | 需要额外模型或双摄像头方案，与「跑通本地识别闭环」不是同一件事 | 明确承认：拿照片对着镜头同样会通过。文档与演示中如实说明，不宣传安全性 |
| 云端人脸库、多设备同步 | 要先解决配网、断网重连、账号体系，其中任何一项的调试成本都超过本地识别本身 | 名单完全在设备内存里，不依赖网络；离线反而让演示更可靠 |
| 手机 App、Web 管理后台、账号系统 | 属于「给已有产品做配套」，本项目还没有产品；且会引入前端一整套新知识 | 所有交互通过设备自身状态机完成，不存在第二个界面 |
| 大规模名单（多人、批量注册） | 名单越大，误认风险越高、阈值越难调，调试成本非线性上升 | 本期只录 1 人（见 2.2）。「只认识一个人」已足以证明识别能力 |
| 人脸照片云端存储、访客统计、考勤报表 | 属于产品运营能力，需要先有数据和后端才能谈 | 不做数据留存，也就没有隐私存储问题要处理 |
| 自训练模型、模型量化调优、从零实现识别算法 | 这是算法工程方向，不是 28 天零基础项目该碰的层次；本期真正的学习目标是理解「检测到脸」和「识别到人」的区别 | 使用现成的检测与识别能力，把时间花在把链路接通、调通上 |
| 复杂触摸 UI、多级菜单 | 触摸交互会引入一整套 UI 状态管理，收益只是「看起来高级」 | 4 个状态之间自动流转（见 2.1），不依赖用户点按 |
| OTA 升级、生产级日志系统、防拆与加密存储 | 都是产品化阶段的问题，在功能还没跑通时引入只会增加失败点 | 固件直接重新烧录；日志仅在开发期间使用，不作为产品功能交付 |

### 3.2 本期砍掉，但排到后续版本

这几项不是「不该做」，而是**排序问题**——它们让产品更完整，但不做也能通过本期验收。

| 功能 | 为什么这期不做 | 什么时候值得做 |
|---|---|---|
| 名单掉电保存（重启后名单还在） | 本期只录 1 人，重录成本只有几十秒；而写入 Flash 涉及存储介质与写入方式的调试，零基础容易卡在这里，风险收益不成比例 | 名单增加到 2–3 人、重录开始变得烦人之后 |
| 多人名册（2–3 人） | 需要多轮录入流程加更谨慎的阈值调整，会显著拉长调试时间 | 单人闭环稳定跑通后，作为第一个增强项 |
| 人员管理（删除 / 重录 / 查看名单） | 名单只有 1 人且掉电即清空，「管理」没有实际对象 | 与掉电保存一起做才有意义 |
| 网络传图 / 远程查看画面 | 需要额外解决网络与服务端，不属于本地闭环 | 本地闭环稳定后，作为扩展演示 |

### 3.3 砍掉的总代价（如实记录）

做完本期，这个设备的真实能力边界是：**只认识 1 个人、重启即忘、认不出的人显示未知、安静环境下才准、拿照片能骗过它**。

把它当「证明这条技术链路在本地跑得通的教学演示器」是准确的；把它当「门禁」是不准确的。

---

## 4. 验收标准

> 通用前提：室内常规照明、人脸正对镜头、距离约 30–80 cm、单人依次上前。
> 以下每条都能当场复现——照做一遍，看屏幕和板子即可判定，不依赖主观感觉。

| 编号 | 验收标准 | 怎么验证（当场可做） | 对应功能 |
|---|---|---|---|
| A1 | 连续重新上电 5 次，每次都能进入待机状态并显示实时画面 | 拔电、插电，重复 5 次，每次都看到待机画面 | F1 F7 |
| A2 | 画面中出现人脸时，屏幕上能框出人脸位置 | 坐到镜头前，看屏幕是否出现人脸框 | F2 |
| A3 | 上电后能完成 1 名人员的录入，且屏幕全程给出「录入中」与进度 | 上电 → 坐定 → 看到录入完成的屏幕提示 | F3 F7 |
| A4 | 已录入人员连续识别 10 次，至少 8 次显示出正确的人员编号 | 按 4.3 的测试规程执行：坐定 → 等到稳定结果 → 离开 → 等 3 秒 → 再坐定，重复 10 轮并记录 | F4 F6 |
| A5 | 未录入人员出现在镜头前，屏幕显示「未知」，且不显示任何人员编号 | 换一个**从未录入过**的人上前，看屏幕；同样执行 3 次 | F5 F6 |
| A6 | 识别成功与失败给出**两种可区分**的 LED 与蜂鸣器模式，且都与屏幕结果一致 | 成功、失败各复现 3 次，记录 LED 表现与响声次数是否稳定一致（模式定义见 4.4） | F8 |
| A7 | 屏幕上的结果文字，在 1 米外仍能看清是什么状态 | 退到 1 米外，判断能否读出屏幕内容 | F6 |
| A8 | 断电重启后，设备行为与首次上电一致（名单清空、回到待机），不存在残留错误状态 | 完成一次识别 → 拔电 → 插电，看是否正常回到待机 | F3 F7 |
| A9 | 能说清人脸数据当前存在哪里、设备重启后它会怎样 | 口头说明，并在屏幕上复现「重启后需重新录入」 | 2.2 |

### 4.1 判为不通过的情形

- 只「偶尔能认出来」，A4 达不到 8/10。
- 未录入人员被显示为某个已录入人员的编号（最严重的一类错误）。
- 失败时屏幕、LED、蜂鸣器三者表现不一致。
- 需要靠「重新烧录固件」才能回到正常状态。
- 只能在本人调试环境下跑通，换个桌子或多一个人围观就失效。

### 4.2 本期不验收的能力

- 不验收准确率上限：8/10 是本项目的工程验收阈值，不是算法能力指标。
- 不验收复杂光照、侧脸、口罩、多人同时在场。
- 不验收任何安全属性：不测防照片、不测防拆、不做安全认证。

### 4.3 测试规程（A4、A5、A6 必须按此执行）

只有「标准 + 规程」加起来才叫可复现。同一份标准，换一套做法会得出完全不同的结论。

**固定条件（每次测试前逐条确认）**

- 环境：室内常规照明，不开闪光灯，不做逆光。
- 位置：人脸与设备距离 30–80 cm，正对镜头。
- 人员：**A4 的测试者必须是录入时的那一个人**；**A5 的测试者必须是从未录入过的人**。
- 一次测试内不改动设备摆放位置（可以微调自己的站位，但每次都要记录是否调过）。

**A4 循环动作（10 轮，必须一次连续做完，中途不重启、不改设置）**

| 步骤 | 动作 |
|---|---|
| 1 | 坐定，等屏幕上出现**稳定的**判定结果（不数瞬时闪过的画面） |
| 2 | 记录这一次是「欢迎，1 号」还是「未知」 |
| 3 | 完全离开画面，直到屏幕回到待机 |
| 4 | 等 3 秒，再坐定，进入下一轮 |

**计数规则（这几条决定 8/10 怎么数）**

- 按**屏幕最终稳定状态**计数，不按瞬时状态计数；抖动之后锁定的结果算最终结果。
- 一轮内同时出现过成功和未知的，该轮算**不通过**（这正是要暴露的问题）。
- 允许记录失败原因（光照变了、没坐正），但**不允许因此作废某轮重新计数**——作废等于偷偷放宽标准。

**A6 重复性要求**

- 成功与失败各复现 3 次，3 次的表现必须一致；「只出现过一次」不算通过。

### 4.4 验收口径与交付版本

本期只有两个交付版本。**交付时必须先声明采用哪一个**，不允许混着说。

| | V1 标准交付 | V1-D 降级交付 |
|---|---|---|
| 产品输出 | 屏幕是唯一产品输出（符合 1.2、2.1） | 屏幕不可用，由串口输出替代屏幕 |
| 适用条件 | 默认路径 | 仅在屏幕驱动确实做不通时启用（见 5.1） |
| 必须通过的验收项 | **A1–A9 全部**（完整完成时的口径；有未完成项时按 5.2 处理） | **A1–A5、A7–A9**（判定载体见下表），A6 另计 |
| A6 的判定 | 必须通过 | **记为「未完成」**，不改判定方式、不做等价替换 |
| 交付说明 | 无需特别注明 | 必须写明「本版为降级交付，屏幕相关能力未完成」 |

**逐条验收载体对照（V1-D 下每一条怎么验）**

降级版没有屏幕，所以凡是以「屏幕显示」为判定载体的条款，都必须给出串口版表述。**只换载体，不换标准**（唯一例外是 A6，直接记未完成）。

| 编号 | V1 标准交付的判定 | V1-D 降级交付的判定 |
|---|---|---|
| A1 | 5 次上电都进入待机并显示实时画面 | 5 次上电都进入待机，以串口输出确认已进入待机 |
| A2 | 屏幕上框出人脸位置 | 以串口输出确认检测到人脸（含框坐标）；无屏幕框也可通过 |
| A3 | 屏幕全程显示「录入中」与进度 | 以串口输出确认录入进度与录入完成 |
| A4 | 屏幕上显示正确的人员编号，≥8/10 | 串口输出的人员编号正确，≥8/10 |
| A5 | 屏幕显示「未知」且不出现任何编号 | 串口输出「未知」且不出现任何编号 |
| A6 | 必须通过 | **记为「未完成」**（不改判定、不找替代） |
| A7 | 屏幕文字在 1 米外可读 | 改为「串口输出字段清晰、可读出状态与编号」 |
| A8 | 断电重启后回到待机、名单清空 | 同左，以串口输出确认已回到待机 |
| A9 | 口头说明 + 在屏幕上复现「重启后需重新录入」 | 口头说明 + 以串口输出复现「重启后需重新录入」 |

> 在 V1-D 下，屏幕相关功能 F1（画面显示）、F6（结果上屏）、F7（状态指示）**记为未完成**，但它们对应的验收项并未被删除，只是把判定载体换成了串口。这样处理后，V1-D 的验收清单是可当场执行的，不再出现「要求验证一个不存在的屏幕」的情况。

**两条硬规定**

1. **不允许「声称标准交付、实际用串口交付」**。屏幕最终没上，交付版本就必须写成 V1-D。
2. **降级不等于放宽标准**。V1-D 只是把 A7 的验证载体从屏幕换成串口；A6 直接记为未完成，绝不用改判定方式的方式凑通过。

**A6 的模式定义（v1.3 已定稿）**

前置事实：目标板**只有 1 颗单色 LED**（GPIO1），**颜色维度不可用** —— 两种模式只能靠「亮/灭 + 闪烁次数」区分。

| 结果 | LED | 蜂鸣器 |
|---|---|---|
| 识别成功 | **常亮 1 秒** | **短响 1 次**（单次 100 ms） |
| 识别失败 | **闪烁 2 次** | **长响 1 次**（单次 500 ms） |

判定要求是「两种模式可被肉眼和人耳区分」：两者都靠「亮/灭 + 次数」而非颜色区分。若实测发现长短响在演示环境下听不出差别，**允许只调整蜂鸣器时长**；但**不允许**改成「两种模式不可区分」，也不允许放松「三者（屏幕 / LED / 蜂鸣器）结果必须一致」这一条。

### 4.5 F8 的优先级例外（原 P1 标注的修正）

F8 在 2 节里被标为 P1（允许降级），但 4.4 又要求 V1 必须通过 A6 —— 这两者原本是冲突的。**本版明确规定以本节为准**：

- **F8 在 V1 中必须完成**，不适用「P1 可降级」。
- 若最终 F8 未完成，则 **A6 记为「未完成」**，并如实写入交付说明，**不允许通过改动 A6 的判定方式来让它通过**。
- 此时仍走 V1 交付路径，但交付说明中必须逐条列出未完成项（见 5.2）。

---

## 5. 风险与降级方案

| 风险 | 可能性 | 影响 | 降级方案（提前定好，卡住时直接执行） |
|---|---|---|---|
| 人脸检测 / 识别能力在目标板上跑不通或稳定性差 | 中 | 整个闭环无法成立 | 优先按「能检测到人脸并框出」验收（F2）；识别部分记为未完成并如实说明，不假装完成 |
| 屏幕驱动调试耗时超预期 | 中 | F1 / F6 / F7 无法按标准交付 | 执行 5.1 的降级路径，交付版本改为 **V1-D**（验收范围与判定见 4.4）；禁止「一边声明屏幕是产品输出、一边用串口交付」 |
| 识别阈值难调，A4 卡在 8/10 以下 | 中高 | 核心验收项不通过 | 固定演示条件（位置、光照、距离），把可识别边界写进文档；如实标注成功率，不放宽标准 |
| 结果只走屏幕，出错时缺少排查线索 | 中 | 排错时间被拉长 | **开发期间保留串口输出作为调试手段**：它是开发工具、不列为产品功能、不参与验收判定；产品对外行为仍只认屏幕结果 |
| 「上电即录」导致录入流程反复触发 | 低 | 演示时体验混乱 | 降级为固定演示流程：上电后按一次按键进入录入，再开始识别；只改触发方式，不改功能定义 |
| 时间不够，做不完 P1 项 | 中 | F7 / F8 缺失 | 优先保 F1–F6（P0）；F7 由屏幕文字兼任，F8 若未完成，**A6 直接记为「未完成」**（不改判定方式）。未完成项如实列入清单，不凑「全部完成」 |
| 设备长时间运行发热、重启 | 低 | 演示中断 | 单次演示控制在 30 分钟内，中间允许断电冷却；连续运行测试排在排期靠后，不作为本期验收项 |

### 5.1 屏幕不可用时的降级路径（→ 转为 V1-D 交付）

若到排期后段屏幕驱动仍做不通，且判断继续投入不划算，按以下顺序执行，不能跳步：

1. 交付版本改声明为 **V1-D 降级交付**（定义见 4.4）。
2. 验收范围收窄为 A1–A5、A7–A9；**A6 记为「未完成」**。
3. 在交付说明中写明：屏幕相关能力（F1 画面显示、F6 结果上屏、F7 状态指示）未完成。
4. 串口输出在此版本中**从开发手段升级为产品输出**——这是全局唯一的例外，且必须写在交付说明里，不能默默执行。

### 5.2 时间被压缩时的最低可交付（降级线）

若时间被严重压缩（而非屏幕坏），**最低可交付**定义为：

> 上电 → 屏幕显示实时画面 → 录入 1 人 → 已录入人员上屏显示「欢迎，1 号」→ 未录入人员上屏显示「未知」。

即只保 F1、F3、F4、F5、F6 这 5 项（对应降级要求中的「首页、详情、一项写入功能」）。此时**仍走 V1 交付路径**（屏幕仍是产品输出，不等于 V1-D），但必须遵守：

1. **不得宣称通过 A1–A9**：未完成项必须逐条列出——F7 未完成、F8 未完成（对应 **A6 记为「未完成」**），其余内容列入后续版本并明确标注。
2. **交付说明中必须写明**：`本版未完成：F7 状态指示、F8 硬件反馈（A6 未完成）`。其余 A1–A5、A7–A9 通过。
3. **版本命名规则**：交付版本按**实际通过的验收集合**命名，而不是按计划命名。
   - 屏幕可用 + A1–A9 全过 → 写 **V1**
   - 屏幕可用 + 只过 A1–A5、A7–A9（A6 未完成）→ 写 **V1（未完成项已列出）**，不得简写为 V1
   - 屏幕不可用 → 写 **V1-D**（见 4.4、5.1）

---

## 6. 开发约定（不属于产品功能）

- 本文的规则与验收判定默认以**屏幕**为准（V1）；仅当采用 V1-D 时，由串口取代屏幕（见 4.4）。
- 除 V1-D 这一例外，串口输出都只是**开发期调试手段**：可随时调整，不参与验收判定。
- 技术选型（确切板型、摄像头型号与接口、屏幕型号与分辨率、LED / 蜂鸣器 / 按键的引脚、是否有 PSRAM、供电与烧录方式、框架、库、模型、分区与存储方案）由 Day 5 单独产出，本文不做限定，**也不从 PRD 里猜**。
- Day 5 已产出**实现契约文档**，定名为 **`TECH_DESIGN.md`**（原暂定名 `IMPLEMENTATION_CONTRACT.md` 作废，两者是同一个东西，不并存）。它用于把本文的暂定值定稿，已覆盖：① 确切硬件型号；② 摄像头 / 屏幕 / LED / 蜂鸣器 / 按键接口；③ 图像分辨率、目标帧率与内存约束；④ 录入采样数量与超时；⑤ 识别阈值与连续匹配判定规则；⑥ 人脸离开与重新触发规则；⑦ 四状态完整转移表；⑧ 初始化失败与运行时异常处理；⑨ 成功 / 失败的 LED 与蜂鸣器模式；⑩ 可执行的验收测试脚本；⑪ V1 与 V1-D 的差异。**本文只指向它，不重复它的内容。**
- 逐项覆盖度的核对表见 `TECH_DESIGN.md` 第 10 节。截至 v1.3，③–⑩ 已全部回填，**唯一仍待实测的是 ③ 里的「片内 SRAM 内存余量」**。
- 本文档若与 `research.md` 冲突，**以本文为准**；`research.md` 作为背景研究保留。

---

## 7. 待确认事项与定稿状态

| 事项 | 当前写法 | 状态 |
|---|---|---|
| 录入的触发方式 | 「上电后首次检测到人脸即进入录入」（因名单为空且只有 1 人） | ⬜ **仍是唯一未决的产品项**。硬件上两条路都可行：KEY0–KEY3 四个按键全部接在 XL9555 上，可支持「先按一次键再录入」；不改则走 2.1 的自动触发。若改为按键触发，执行第 5 节对应降级方案 |
| 2.3 表中 R1–R10 的数值 | 已定稿，见 2.3 表 | ✅ **v1.3 已定稿回填**。其中 R3 / R5 的毫秒值为**推算**（由 R4 的「15 帧 ≈ 1 秒」换算而来），实测帧率后需复核 |
| A6 的 LED / 蜂鸣器具体模式 | 成功 = 常亮 1 秒 + 短响 1 次；失败 = 闪烁 2 次 + 长响 1 次 | ✅ **v1.3 已定稿**（见 4.4）。板载确认为 1 颗单色 LED，颜色维度不可用；蜂鸣器时长定为短 100 ms / 长 500 ms |
| R9 多人时取「面积最大的脸」 | 只处理检测框面积最大的那一张 | ✅ **v1.3 已确认可行**：检测结果含 `box[4]`（x1, y1, x2, y2），面积可自行算出。**但要注意**：esp-who 示例自身写的是「恰好检测到 1 张脸才动作」，实现时必须替换成「取最大脸」 |

> 本表未定稿的项，**不影响本文作为产品边界与验收基线使用**；但它们必须在写代码前定稿，否则会变成实现者（AI 或人）自行假设的参数。v1.3 起，定稿值统一放在 `TECH_DESIGN.md` 第 4 节，本表只登记状态。
