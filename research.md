# ESP32 人脸识别：同类产品研究与 28 天最小版本建议

## 1. 研究范围与结论摘要

本研究把“类似产品”定义为：能在边缘设备上完成摄像头采集、人物/人脸检测或识别，并能被初学者用于做原型的开发板或模块。比较对象为：

1. Espressif ESP-WHO + ESP32-S3-EYE
2. M5Stack UnitV2 AI Camera
3. OpenMV Cam H7 Plus

核心结论：零基础在 28 天内最适合做“单设备、少量已录入人员、本地识别、屏幕/串口反馈”的 MVP。ESP32-S3-EYE 与目标硬件最贴近；UnitV2 可作为“先跑通效果”的参考；OpenMV 适合学习机器视觉脚本，但不应作为本项目的硬件基线。

## 2. 产品比较

| 产品 | 目标用户 | 核心页面/入口 | 主要数据 | 明显优点 | 明显不足 |
|---|---|---|---|---|---|
| **ESP-WHO + ESP32-S3-EYE** | 使用 ESP-IDF/ESP-WHO 做 AIoT 原型的嵌入式开发者；适合希望最终做成 ESP32 固件的人 | 官方示例/固件测试流程；板载 LCD；摄像头实时识别界面；ESP-IDF 工程与烧录流程 | 人脸框、检测/识别结果、人员 ID；摄像头图像；可扩展音频/按键/IMU/SD 卡/Wi‑Fi 图像传输 | 与 ESP32 目标平台一致；官方提供人脸检测/识别示例；ESP32-S3-EYE 集成 2MP 摄像头、LCD、麦克风、8MB PSRAM、8MB Flash，能直接做门铃、考勤等 AIoT 原型 | 依赖 ESP-IDF、模型、分区和烧录链路，零基础调试成本高；官方指南明确指出没有 USB-UART bridge，程序持续重启时可能难以再次烧录 |
| **M5Stack UnitV2 AI Camera** | 想快速验证视觉功能、通过浏览器/串口接入主控的创客和应用开发者 | `unitv2.py` 或 `IP:10.254.239.1` 浏览器预览页；功能导航栏；UART 控制/输出；Jupyter、SSH/Wi‑Fi 配置页 | JSON 识别结果；人脸框 `x/y/w/h`、置信度 `prob`、五点关键点；人脸识别信息；连续 UART 115200 输出 | 即插即用；内置多种识别服务；浏览器可看预览；识别内容自动 JSON 输出，便于被 ESP32/PC 调用；官方文档给出 Face Detector 与 Face Recognition 的样例数据 | 它是独立 Linux AI 模块而非 ESP32 原生方案；需要通过网络/UART 集成，硬件与软件栈更重；浏览器兼容性有要求，官方建议 Edge/Chrome，Firefox 可能卡顿或视频不稳定 |
| **OpenMV Cam H7 Plus** | 学习机器视觉、希望用 MicroPython/IDE 快速写视觉脚本的创客和教学用户 | OpenMV IDE 连接页；脚本运行/串口输出；机器视觉 API；摄像头与可替换镜头/传感器入口 | 图像帧、检测框/关键点、FPS；MicroPython 脚本中的视觉结果；可通过 I/O 控制外设 | Python 而非 C/C++，适合初学者；官方文档提供 IDE 三步上手；算法在设备本地运行，不需要主机或云；H7 Plus 具备 480MHz Cortex-M7、32MB SDRAM、5MP OV5640，简单算法在 QVGA 可达约 25–50 FPS | H7 Plus 官方页面标注“不再生产”，仅保留文档并推荐后继型号；它不是 ESP32 平台；公开资料更偏通用机器视觉，做 ESP32 产品时仍需重新适配硬件和固件 |

### 2.1 目标用户差异

- **ESP-WHO**：目标是“嵌入式产品原型和 ESP32 AIoT”，学习成本换取平台一致性。
- **UnitV2**：目标是“快速得到可调用的识别结果”，把模型和推理封装起来，降低算法门槛。
- **OpenMV**：目标是“用 Python 学机器视觉并控制 I/O”，重心是脚本体验和可实验性。

### 2.2 核心页面/交互差异

- ESP-WHO 的核心入口是开发工程、板载 LCD 和默认固件测试，偏“设备端产品体验”。
- UnitV2 的核心入口是浏览器预览页、功能导航和 JSON/UART，偏“模块服务体验”。
- OpenMV 的核心入口是 IDE、脚本和 API 文档，偏“开发工具体验”。

### 2.3 主要数据差异

- ESP-WHO/ESP32-S3-EYE：更接近最终设备需要的“识别状态 + 本地 UI/外设动作”。
- UnitV2：最明确地结构化输出 JSON，包括坐标、置信度、关键点和识别信息。
- OpenMV：以脚本对象和图像处理结果为主，适合自定义算法流程，但需要自行设计产品级数据结构。

## 3. 适合零基础的 28 天 MVP

### 3.1 MVP 定义

**项目名：ESP32 本地人脸识别门禁演示器**

目标不是做“通用门禁产品”，而是在一块 ESP32 摄像头板上稳定演示完整闭环：

1. 摄像头实时取帧。
2. 识别 1–3 名预先录入的人员。
3. 识别成功后在屏幕或串口显示“已识别 + 人员 ID”。
4. 未知人显示“未知”。
5. 通过 LED 或蜂鸣器给出成功/失败反馈。
6. 设备重启后仍能读取已保存的人员配置（优先使用 NVS/Flash；若本阶段风险过高，先接受固定演示名单并把持久化列为最后验收项）。

### 3.2 建议的最小页面/状态

MVP 只保留 4 个状态，不做复杂 App：

| 状态 | 用户看到什么 | 主要数据 |
|---|---|---|
| 待机 | 摄像头画面/“请看镜头” | 当前帧、设备状态 |
| 录入 | “录入第 N 人”与进度 | 人员 ID、采样计数、特征/模型数据 |
| 识别成功 | “欢迎，ID N” | 匹配 ID、置信度/阈值结果、时间 |
| 识别失败 | “未知人员” | 检测结果、失败计数 |

### 3.3 28 天安排

| 天数 | 目标 | 验收证据 |
|---|---|---|
| 1–3 | 确认硬件、供电、摄像头和串口日志 | 能稳定启动并看到日志 |
| 4–7 | 跑通官方摄像头/人脸检测示例 | 画面中能检测到人脸框 |
| 8–11 | 理解识别示例、阈值和模型文件 | 能区分“检测到脸”和“识别到人” |
| 12–15 | 做单人录入流程 | 能录入 1 人并在内存中匹配 |
| 16–19 | 扩展到 2–3 人，增加未知分支 | 已知/未知结果可重复演示 |
| 20–22 | 加入 LED/蜂鸣器和简单状态显示 | 成功、失败反馈与状态一致 |
| 23–25 | 加入 Flash/NVS 保存与重启恢复 | 重启后名单仍可用，或记录明确降级方案 |
| 26–27 | 光照、距离、角度和连续运行测试 | 形成测试记录，标出可识别边界 |
| 28 | 整理演示流程和文档 | 一次连续演示成功，保存日志/视频证据 |

### 3.4 MVP 验收标准

- 连续重启 5 次，设备都能进入待机状态。
- 在固定光照、固定距离下，已录入人员连续识别 10 次，结果至少 8 次正确（这是本期工程验收阈值，不是算法安全保证）。
- 未录入人员能显示“未知”，而不是默认放行。
- 识别成功和失败有不同的 LED/蜂鸣器反馈。
- 能说明识别数据存在哪里、如何删除或重新录入。
- 能列出光照、角度、距离对结果的影响；不宣称具备真实门禁安全性。

## 4. 本期不做

为保证 28 天可完成，本期明确不做：

- 手机 App、账号系统、云端后台和远程管理。
- 多设备联网、跨设备人员同步和云端人脸库。
- 活体检测、照片攻击防护、口罩/侧脸/强逆光等复杂鲁棒性。
- 大规模人员库、自动批量注册和复杂权限/角色管理。
- 门锁、电机、继电器等高风险执行器的真实门禁控制；只做 LED/蜂鸣器演示。
- 人脸照片云存储、行为画像、访客统计和长期考勤报表。
- 自训练深度模型、模型量化优化和从零实现识别算法。
- Web 管理后台、复杂触摸 UI、OTA 升级和生产级日志系统。
- 防拆、加密存储、隐私合规审计和商用安全认证。

## 5. 产品选择建议

如果目标是学习并最终交付“ESP32 人脸识别”，优先选 **ESP32-S3-EYE + ESP-WHO 官方示例**作为基线；它与目标平台一致，能直接验证摄像头、LCD、识别和外设闭环。可借鉴 UnitV2 的 JSON 字段设计和“识别结果与控制逻辑解耦”思路；可借鉴 OpenMV 的分阶段脚本/IDE 学习方式，但不把 H7 Plus 作为采购基线，因为官方已经标注该型号不再生产。

## 6. 公开参考资料

以下资料均为公开页面，访问/抓取时间为 2026-09-18：

1. Espressif ESP-WHO GitHub（框架概览、示例与支持开发板）：<https://github.com/espressif/esp-who>
2. Espressif ESP32-S3-EYE Getting Started Guide（硬件、默认固件、功能测试、已知问题）：<https://github.com/espressif/esp-who/blob/master/docs/en/get-started/ESP32-S3-EYE_Getting_Started_Guide.md>
3. M5Stack UnitV2 产品文档（硬件规格、浏览器预览、JSON/UART、开发入口）：<https://docs.m5stack.com/en/unit/unitv2>
4. M5Stack UnitV2 Built-in Recognition Service（Face Detector/Face Recognition 页面与样例 JSON）：<https://docs.m5stack.com/en/guide/ai_camera/unitv2/base_functions>
5. OpenMV Cam H7 Plus 产品页（处理器、摄像头、性能、停产说明）：<https://openmv.io/products/openmv-cam-h7-plus>
6. OpenMV MicroPython 文档（IDE 快速开始、本地推理、视觉 API 与隐私说明）：<https://docs.openmv.io/>

> 说明：文中的“明显不足”和 28 天排期是基于上述公开事实做的项目规划判断，不是厂商对竞品的评价；识别准确率会受光照、姿态、距离、镜头和模型版本影响，不能把本 MVP 当作安全认证或商用门禁方案。
